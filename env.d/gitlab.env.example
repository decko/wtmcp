# GitLab plugin configuration
#
# Copy this file to gitlab.env and uncomment the variables you need:
#   cp gitlab.env.example gitlab.env
#
# Authentication is handled by the core HTTP proxy. Tokens are
# not passed to the plugin process.
#
# Supports multiple instances via per-domain auth binding.

# --- Single instance ---
#GITLAB_URL=https://gitlab.com
#GITLAB_TOKEN=your-personal-access-token

# --- Multiple instances ---
# Configure each instance with a name:
#
#GITLAB_PUBLIC_URL=https://gitlab.com
#GITLAB_PUBLIC_TOKEN=your-public-gitlab-token
#
#GITLAB_INTERNAL_URL=https://gitlab.internal.example.com
#GITLAB_INTERNAL_TOKEN=your-internal-gitlab-token

# TLS: to skip hostname verification for self-signed certificates,
# configure services.http.tls.skip_hostname_verify in plugin.yaml
# instead of using GITLAB_SSL_VERIFY (which is no longer supported).
